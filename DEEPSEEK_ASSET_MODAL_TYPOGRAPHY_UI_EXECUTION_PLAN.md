# FRAMEFLOW 资产显示、弹窗完整性与字体/UI 优化执行计划

## 0. 执行身份与目标

本任务由 DeepSeek Harness 在以下工作区内直接实施：

`D:\11067\Codex\2026-08-13\video-2`

目标是一次性解决三个相互关联的问题，并形成可验证、可回归的完整闭环：

1. 资产上传成功且状态完整，但工作台看不到实际图片、视频或音频。
2. 多个弹窗内容贴边、被边框裁切、底部操作不可见或需要无提示地滚动。
3. 页面字体、字号、字重和视觉层级不统一，需要在保持现有暗色产品风格的前提下完成全局 UI 优化。

任务不是制作静态设计稿，而是修改真实代码、补齐测试、启动页面并完成交互验收。

## 1. 强制约束

- 当前工作树包含大量用户已有修改和未跟踪文件，必须增量修改，禁止覆盖、回退或清理无关内容。
- 禁止运行 `git reset --hard`、`git checkout --`、`git clean` 或任何批量删除命令。
- 禁止删除 `data/` 中的用户资产。
- 不得伪造 QA、Ready、登记或其他业务状态。
- 不得把本地绝对文件路径直接转换为 `file://` URL；媒体必须通过受控的 `/api/project-files/...` 路由访问。
- 不引入依赖公网加载的字体、图标或 CSS。工作台必须保持离线可用。
- 不提交 Git commit，不推送远端。
- 每完成一个阶段先运行相关测试，再进入下一阶段。
- 若发现现有代码与计划冲突，优先保持业务状态机和安全边界，再在最终报告中说明偏差。

## 2. 当前已确认的根因

### 2.1 资产媒体不显示

- `POST /api/assets/intake` 顶层响应返回了媒体 URL。
- `GET /api/assets/intake` 使用 `asset_audit.artifact_payload()`，列表记录没有 `url`。
- 前端上传完成后立即重新请求列表，上传响应中的 URL 被丢弃。
- `auditRow()` 只有在 `m.url` 存在时才渲染图片，因此缩略图分支长期无法进入。
- `assetCard()` 的 `.asset-visual` 永远是 CSS 类型占位图，没有读取 `filePath` 或 artifact URL。
- `inspectorBody()` 只显示“文件已存在、artifactId、QA”，没有实际媒体预览。
- `bind()` 调用 `loadAudit()` 后，仅在存在 `scrollTarget` 时刷新审计区域。真实项目可能有 30 条记录，但首次进入显示 0 条，点击标签后才出现。

关键文件：

- `server.py`
- `frameflow/asset_audit.py`
- `api.js`
- `asset-intake-state.js`
- `asset-workspace.js`
- `styles.css`
- `tests/test_asset_intake.py`

### 2.2 弹窗内容不完整

- 基础 `dialog` 为 `padding: 0`，只有 `dialog form` 有内边距。
- 多数动态弹窗直接把 `.dialog-head`、字段和操作区插入 `dialog`，没有 `<form>` 或统一正文容器，所以内容距边框约 1px。
- 资产上传窗口在约 1418×802 的页面视口下，底部操作初始超出弹窗约 30px；1024×640 时约 173px；768×560 时约 243px。
- 设置窗口正文约 1413px，高度远超可视区域，标题与“完成”按钮未形成固定头尾结构。
- 资产详情使用内层 `.inspector-grid` 滚动，外层弹窗也参与布局，窄窗口下底部登记、版本和 Agent 操作不在初始可见区域。
- 审计详情、映射、整改、版本历史虽能滚动，但标题、卡片和按钮仍贴边。

涉及窗口至少包括：

- 新建项目
- 工作台设置
- 项目管理
- 资产上传
- 资产详情
- Prompt QA
- 审计详情
- 资产映射
- 整改/隔离处理
- Prompt 修订或重建
- 资产版本历史
- 脚本版本历史
- 脚本优化
- 分镜候选审阅
- 镜头资产详情

关键文件：

- `index.html`
- `styles.css`
- `asset-workspace.js`
- `story-workspace.js`
- `project-manager.js`

### 2.3 字体和 UI 层级不统一

- 当前全局字体栈和品牌字体混用 `Inter`、`Georgia`、系统中文字体与硬编码等宽字体。
- `Inter` 并未在本地提供，实际呈现依赖系统回退，不同控件和中文文本观感不一致。
- 大量 8px/9px 文本可读性偏低，标题、正文、辅助文字之间的层级不足。
- 卡片、按钮、输入框、弹窗、焦点态和悬停态缺少统一设计令牌。
- 必须保留 FRAMEFLOW 当前暗色、荧光黄绿色主色和生产控制台气质，不能改造成完全不同的产品。

## 3. 当前工作树特别说明

Codex 在本任务中断前已经对以下位置做了初步但未完成的增量修改：

- `index.html`：为新建项目和设置窗口加入了 `.dialog-body` 与 `.dialog-footer` 结构。
- `styles.css`：加入中文优先字体变量、基础弹窗头/正文/底部规则、部分资产媒体样式。

DeepSeek Harness 必须先阅读实际 diff，验证这些修改是否正确，再继续完善。可以调整或替换局部实现，但不得把整个文件回退到 Git 基线，也不得丢失其他用户改动。

## 4. 实施阶段

### 阶段 A：建立基线与保护范围

1. 执行 `git status --short` 和相关文件的 `git diff`。
2. 阅读 `AGENTS.md`（若工作区存在）、`README.md`、`package.json` 和现有测试入口。
3. 记录本任务开始时的文件状态，不修改无关文件。
4. 运行基线测试：
   - `npm test`
   - `python -m unittest discover -s tests -p "test_*.py" -v`
5. 如果基线失败，区分已有失败与本任务引入失败，不得为了“全绿”删除测试。

### 阶段 B：修复后端媒体 URL 合同

1. 统一资产响应结构，使列表、单项详情、上传完成和登记后的 artifact 都能提供浏览器可访问的 `url`。
2. URL 必须由 `artifact_url(project_id, Path(local_path))` 或等价安全方法产生。
3. 不向浏览器暴露可直接访问的任意文件系统 URL。
4. 对文件不存在、路径越界和不属于当前项目的情况保持现有安全校验。
5. 为 `GET /api/assets/intake` 增加测试，至少断言：
   - 上传后列表记录存在 `url`。
   - URL 以对应项目的 `/api/project-files/{project_id}/...` 开头。
   - 请求该 URL 能返回 200 和正确媒体类型。

### 阶段 C：前端资产媒体展示

1. 建立单一媒体选择逻辑：
   - 优先当前 active/已登记 artifact。
   - 其次最新 qualified 候选。
   - 再其次最新 intake/unqualified 候选，并明确显示候选状态。
   - 不得因为有候选文件就把逻辑资产误判为 Ready。
2. 图片使用 `<img>`；视频使用受控 `<video>`；音频使用 `<audio>` 或明确的音频卡片。
3. 在以下区域显示真实媒体：
   - 审计列表缩略图。
   - 逻辑资产卡片主视觉。
   - 资产详情的大图/视频/音频预览。
4. 提供加载失败回退：格式图标、文件类型和“无法加载预览”，不能出现破图占位。
5. 媒体 URL、名称和属性必须经过现有 `esc()` 或安全 DOM 赋值。
6. 上传成功后立即刷新当前项目审计数据和资产卡片，不依赖用户再次点击标签。
7. 修复项目切换时旧项目审计记录短暂混入新项目的问题，避免异步竞态覆盖当前项目。
8. 图片采用合理 `object-fit`：卡片可 `cover`，详情应 `contain`。
9. 视频默认不得自动播放或自动发声。

### 阶段 D：统一弹窗架构

目标结构：

```text
dialog
├─ 固定/粘性标题栏
├─ 唯一可滚动正文区
└─ 固定/粘性底部操作栏
```

1. 建立可复用 CSS 类，例如 `.dialog-head`、`.dialog-body`、`.dialog-footer`。
2. 动态弹窗必须获得 18–24px 桌面内边距和 14–16px 窄屏内边距。
3. 每个弹窗只保留一个主要纵向滚动容器；删除资产详情等位置的双层滚动。
4. 标题栏关闭按钮始终可见。
5. 有关键提交操作的窗口，其底部操作栏始终可见；正文滚动时不能被内容覆盖。
6. 设置窗口必须采用固定标题、滚动正文、固定“完成”按钮。
7. 长文件路径、artifact ID、JSON/QA 报告必须换行或在内部安全滚动，不得撑破窗口。
8. `form-grid` 在窄屏切为单列。
9. 不得让 sticky footer 遮挡正文最后一项。
10. 使用键盘 Esc 和关闭按钮都能正常退出；不要破坏已有 `cancel` 事件处理。

### 阶段 E：统一字体与视觉系统

1. 使用本地系统字体栈，不下载网络字体：
   - UI/中文：`Segoe UI Variable`、`Microsoft YaHei UI`、`PingFang SC`、`Noto Sans CJK SC`、`Source Han Sans SC`、`sans-serif`。
   - 代码/ID：`Cascadia Code`、`SFMono-Regular`、`Consolas`、`monospace`。
2. 品牌标记不再单独使用与正文冲突的衬线字体，除非能证明这是有意的品牌处理且不会造成不统一。
3. 建立清晰字号层级，建议下限：
   - 页面标题 28–38px。
   - 弹窗标题约 18–20px。
   - 卡片标题 12–14px。
   - 正文 12–14px。
   - 辅助文字原则上不低于 10px；关键状态不得用 8px。
4. 统一字重、行高、字距和中英文混排。
5. 输入框、按钮、select、textarea 必须继承统一字体。
6. 优化焦点态、悬停态、禁用态，确保键盘操作可辨识。
7. 适度统一卡片阴影、边框、圆角和间距，不改变核心品牌配色。
8. 避免为了“美观”堆叠过度动画；动画应短且尊重 `prefers-reduced-motion`。

### 阶段 F：响应式与真实交互验收

至少在以下页面视口检查：

- 1418×802（对应常见 1440×900 浏览器工作区）
- 1024×640
- 768×560

逐个真实点击并检查：

1. 新建项目。
2. 工作台设置。
3. 项目管理。
4. 资产上传。
5. 资产详情。
6. 审计详情。
7. 映射窗口。
8. 整改窗口。
9. 版本历史。
10. 脚本历史和脚本优化。
11. 能通过现有数据触发的 Prompt QA、审阅和镜头详情。

每个窗口必须满足：

- 左右内容不贴边、不越过边框。
- 标题和关闭按钮可见。
- 正文完整可滚动。
- 主操作按钮可见或通过明确滚动区到达，优先固定显示。
- 无横向页面溢出。
- 无控制台错误。
- 关闭后焦点和页面可继续操作。

资产页面必须额外满足：

- 首次进入即显示正确的审计数量。
- 图片资产能显示缩略图和详情预览。
- 刷新页面后预览仍存在。
- 项目切换后不显示上一项目的审计记录。
- 上传成功仍然保持“上传不等于 Ready”的状态规则。

## 5. 自动化测试要求

必须执行并记录：

```powershell
npm test
python -m unittest discover -s tests -p "test_*.py" -v
```

需要新增或扩展测试覆盖：

- intake 列表 URL 合同。
- 项目文件 URL 可访问性。
- 媒体类型判断或媒体视图模型的纯函数测试；如当前前端测试结构不适合 DOM 测试，至少把选择逻辑提取为可测试纯函数。
- 项目切换/异步加载不应让旧项目数据覆盖新项目。
- 现有状态机测试必须继续通过。

不得只通过修改测试期望来掩盖实现错误。

## 6. 完成定义

只有同时满足以下条件才能宣告完成：

- 三类问题均有代码修复，不是只写说明。
- 资产 URL 从后端到卡片、列表、详情形成闭环。
- 关键弹窗在三种视口下完成真实点击检查。
- 字体系统全局统一且离线可用。
- 自动化测试全部通过，或明确列出与本任务无关的既有失败证据。
- 浏览器控制台没有本任务引入的错误。
- 没有删除、覆盖或回退用户无关改动。
- 生成最终报告 `DEEPSEEK_UI_ASSET_EXECUTION_REPORT.md`，内容包括：
  - 修改摘要。
  - 根因与修复映射。
  - 修改文件清单。
  - 测试命令与结果。
  - 三种视口的交互检查结果。
  - 已知限制和后续建议。

## 7. 执行要求

现在开始实施，不要只回复计划或等待用户确认。先建立基线，再按 B→C→D→E→F 顺序推进。遇到非破坏性的实现细节自行做合理选择；只有在需要删除用户数据、覆盖大量现有修改或改变核心业务规则时才停止并请求确认。

