# FrameFlow AI 视频工作台 V3 全面升级计划

> 文档状态：恢复实施记录  
> 最后更新：2026-08-19  
> 实施状态：阶段 D、阶段 E 已核查，阶段 F、阶段 G 已完成；阶段 H 的本地安全与回归项已完成，真实机器性能基准和真实 Provider 演练待外部条件具备。  
> 工作区说明：本轮将 FrameFlow 定义为全新 V3 产品；V3 运行时不保留旧版界面、旧版入口或旧版 API。旧文件如需清理必须逐个明确处理，旧数据库/媒体不会被批量删除。

## 1. 项目愿景

将现有 FrameFlow 从“固定八阶段 AI 视频生产流水线”演进为面向个人专业创作者的本地优先 AI Video OS，同时支持：

1. **专业流程模式**：保留剧本、资产监管、角色/场景/道具、融合、镜头导演、声音、视频生成、质检与交付的严格门控。
2. **无限画布模式**：通过节点、连接、Agent、模板和并行任务自由编排工作流。
3. **统一项目状态**：流程视图与画布视图共享同一项目、资产、任务、审批和版本数据，不维护两套互相漂移的状态。
4. **云端与本地混合执行**：统一接入 BYOK 云模型、火山方舟、OpenCode、ComfyUI 和其他正式开放 API。
5. **监督式 Agent 自动化**：低成本文本、分析和编排可以自动执行；付费生成、批量任务、外部同步、发布和最终交付必须确认。
6. **可交付后期时间线**：支持多轨剪辑、字幕、音频混合、代理预览和 FFmpeg 确定性导出。

### 已确定的产品选择

- 核心用户：个人专业视频创作者。
- 主要内容：短片、短剧、广告、MV 和具有角色/镜头连续性要求的 AI 影像。
- 交互主轴：固定专业流程与无限画布双模式。
- 前端路线：React、TypeScript、Vite 作为 V3 唯一运行时，直接服务根入口 `/`。
- 后端路线：继续使用 FastAPI、SQLite、系统凭据库和本地媒体目录。
- 模型路线：云端 BYOK 与本地 ComfyUI 混合。
- Agent 边界：监督式自动化。
- 后期深度：可交付多轨时间线，不在第一版复制 Premiere 或 DaVinci Resolve。

## 2. 当前工作台基线

当前系统已经具备以下能力，应作为升级基础保留：

- FastAPI、SQLite WAL、项目版本与冲突检测。
- OpenAI、OpenAI-compatible、OpenCode 和火山方舟供应商配置。
- API 密钥保存到 Windows Credential Manager，不在项目或前端回传明文。
- 故事优化、分镜候选、版本接受与资产监管。
- 角色、场景、道具、融合和声音资产的分级、QA、登记与版本管理。
- 镜头导演包、Seedance 2.0/2.5 执行包、后台轮询、取消和重试。
- OpenAI 图片生成/编辑、TTS、参考声音、对白 Take 与音频 QA。
- FFmpeg 检测、视频拼接、音频混合、最终 MP4 和 manifest。
- 资产导入、技术校验、领域 QA、未合格资产库和失败整改闭环。
- 当前基线测试：后端 41 项、前端 39 项，规划阶段检查时全部通过。

### 当前主要结构性问题

- 前端仍以大体积原生 JavaScript 文件为主，复杂交互继续堆叠会提高维护成本。
- 固定八阶段适合严格生产，但缺少自由编排、分支、复用和局部执行能力。
- 任务、工作流运行、资产版本和前端阶段状态之间仍存在多套表达。
- Provider 接入偏单点实现，缺少统一能力契约、费用估算和输出标准化。
- 缺少显式的节点级输入血缘、运行快照、局部缓存和依赖失效机制。
- 后期仍以拼接和基础音频为主，缺少真正可编辑的多轨时间线。

## 3. GitHub 与成熟产品研究基线

### 3.1 开源项目

#### ComfyUI

参考地址：<https://github.com/Comfy-Org/ComfyUI>

重点学习：

- 节点图与端口化数据流。
- 异步队列和局部重新执行。
- 工作流 JSON、模板、子图和 App Mode。
- 本地 API、模型发现、内存管理和插件节点。
- 生成媒体携带工作流与种子信息的可追溯设计。

不直接复制其 GPL 代码，只学习架构和交互模式。

#### MoneyPrinterTurbo

参考地址：<https://github.com/harry0703/MoneyPrinterTurbo>

重点学习：

- 从主题、剧本、素材检索、配音、字幕到成片的一键生产链。
- 任务轮询、配置预设、快速本地部署和失败恢复。
- 面向非专业用户的最短生产路径。

FrameFlow 不采用其“完全自动成片”作为唯一模式，而是吸收其低操作成本。

#### React Flow / xyflow

参考地址：<https://github.com/xyflow/xyflow>

用途：

- 构建无限画布、节点、连接、分组、缩略图和快捷键。
- 支持自定义节点、选择、连线验证和大画布性能优化。
- MIT 许可证适合作为 V3 画布基础组件。

#### Pireel

参考地址：<https://github.com/pireel/pireel>

重点学习：

- Agent 通过 MCP 驱动视频编辑器。
- 编辑器 UI、合成核心和 Provider 契约分层。
- 浏览器端项目状态、字幕、主题、预览和导出。

该项目为 AGPL，默认只学习架构，不复制实现。

#### ComfyTV

参考地址：<https://github.com/jtydhr88/ComfyTV>

重点学习：

- ComfyUI 工作流导入、输入绑定和阶段预设。
- 故事板、洋葱皮、Animatic、Fountain 剧本和多种导出。
- 图片、视频和音频的统一节点编辑方式。

#### Velorn

参考地址：<https://github.com/VelornLabs/velorn>

重点学习：

- 把 ComfyUI 作为生成引擎，把工作台作为生产管理层。
- 规划、资产、生成、时间线、字幕、效果和导出的统一项目结构。
- Agent/MCP 工具和自定义 ComfyUI 工作流桥接。

#### LibTV Skills

参考地址：<https://github.com/libtv-labs/libtv-skills>

重点学习：

- Agent 会话创建、轮询、结果下载和项目链接交接。
- 上传参考图/视频后继续编辑或生成的标准流程。
- 平台能力以 Skill/CLI 方式暴露给外部 Agent。

#### Remotion

参考地址：<https://github.com/remotion-dev/remotion>

研究用途：程序化视频、React 合成、预览和确定性渲染。

限制：Remotion 使用特殊许可证，个人和小团队可能免费，但公司使用和再分发存在额外要求。第一版不把 Remotion 设为运行时依赖，继续使用 FFmpeg；如未来采用，必须先完成正式许可证审核。

### 3.2 成熟工作流平台

#### LibTV

参考地址：<https://www.liblib.tv/wappro?sourceid=040004>

重点学习：

- 剧本、角色与世界观、无限画布、Agent、Skill 和成片导出的统一入口。
- 节点工作流、创作者作品、模板和社区经验沉淀。
- 角色多角度一致性、素材引用和 AI 导演能力。
- 生成与剪辑一体化，而不是只提供单次模型调用。

#### TapNow

参考地址：

- <https://docs.tapnow.ai/en/docs/canvas/explore-the-canvas>
- <https://docs.tapnow.ai/en/docs/agent/tapnow-agent>

重点学习：

- 每个文本、图片、视频、声音和研究结果都是独立节点。
- 新结果不会覆盖源节点，连接用于表达输入与输出血缘。
- 用户显式选中参考节点，并说明身份、构图、光线或风格等用途。
- Agent 在执行前确认交付物、受众、约束、费用和停止点。
- 局部修订必须明确“只修改什么”和“必须保留什么”。

#### FlowMuse

参考地址：<https://flowmuse.ai/>

重点学习：

- 多模态无限画布与多线程并行生成。
- 文本、图片、视频、声音模型的统一节点生态。
- 个人资产库、生成历史、Prompt 模板和跨项目复用。
- 官方/社区模板、插件扩展和 Agent 自动化编排。

### 3.3 研究评估规则

不能只按 Star 数量决定采用，后续研究应使用以下权重：

- 视频全流程覆盖度：25%。
- 架构与扩展性：20%。
- 维护活跃度：15%。
- 许可证兼容性：15%。
- 本地优先与隐私：15%。
- Star、Fork 和社区采用度：10%。

GPL、AGPL 或特殊商业许可证项目默认只作产品和架构参考；任何第三方代码进入仓库前都要记录来源、版本和许可证。

## 4. 目标信息架构与用户体验

### 4.1 主导航

- 项目总览。
- 专业流程。
- 无限画布。
- 故事与分镜。
- 资产库。
- 镜头导演。
- 声音工作台。
- 生成任务中心。
- 后期时间线。
- 质检与交付。
- Provider 与系统设置。

### 4.2 双模式共享原则

- 专业流程是内置工作流模板的聚合视图。
- 无限画布显示同一工作流模板的节点和连接。
- 在专业流程批准故事、资产或镜头时，对应节点状态同步更新。
- 在画布新增节点或分支时，专业流程将其汇总到对应阶段或“自定义节点”区域。
- 两种视图不得分别保存完成度、审批或资产版本。

### 4.3 画布节点类型

第一版节点类型固定为：

- 创意简报。
- 故事结构/节拍。
- 剧本。
- 场次。
- 镜头。
- 角色。
- 场景。
- 道具。
- Prompt。
- 图片生成。
- 图片编辑。
- 视频生成。
- TTS/对白。
- 音乐。
- 音效。
- 素材融合。
- QA。
- 审批门。
- Agent。
- 时间线。
- 最终导出。

### 4.4 连接语义

- `execution`：参与拓扑调度，必须构成有向无环图。
- `reference`：表达身份、风格、构图、动作、光线等参考，可形成循环但不参与执行。
- `lineage`：表达资产派生关系。
- `annotation`：用于说明、分组或人类备注。

执行连接若形成环，后端必须拒绝保存；用户需要改为参考/注释连接或拆分成新版本节点。

## 5. 分阶段实施方案

## 阶段 A：架构底座与安全迁移

### 目标

建立 V3 数据层、API 和渐进式 React 前端基础，同时保持现有工作台可用。

### 后端改造

- 数据库 Schema 升级到 V3，只新增表和字段，不删除 V1/V2 数据。
- 保留 `projects.document_json` 作为旧格式兼容快照。
- 增加版本化工作流图、图事件、模板、运行、节点运行、审批门、运行事件、资产血缘和时间线表。
- 所有更新使用 `expected_revision` 乐观锁。
- 每次迁移写入 `schema_migrations`，并提供明确回滚脚本。
- 迁移失败时不得修改原项目 JSON 或媒体文件。

### 前端改造

- 新建 React、TypeScript、Vite 工作台。
- 新工作台直接运行在 `/`。
- V3 不提供旧版入口、旧版静态资源或旧版 API；旧源文件不因本次改造批量删除。
- V3 功能不以旧版功能对等作为发布门槛，以 V3 自身的端到端验收作为发布门槛。
- 逐步把 API、项目状态、领域逻辑和 UI 从大文件中拆分为独立模块。

### 验收条件

- 所有现有项目可以打开。
- 迁移不覆盖任何故事、镜头、资产、生成记录或音频。
- 根入口直接打开 V3 工作台，旧版入口返回明确的退役响应。
- 原有 80 项自动测试全部通过。

## 阶段 B：工作流图与统一运行时

### 工作流图

- 支持拖拽、缩放、框选、多选、复制、删除、分组、折叠、自动布局和缩略图。
- 支持节点输入/输出端口、连线验证和自定义节点 Inspector。
- 支持撤销/重做、保存状态和版本冲突提示。
- 内置“专业 AI 视频全流程”模板，将现有八阶段映射为图节点。

### 调度器

- 按执行边进行拓扑排序。
- 支持项目级并发上限。
- 只运行用户选择的节点及必要依赖。
- 输入与配置未变化时复用缓存。
- 修改上游后，只让受影响下游失效。
- 支持排队、运行、暂停、恢复、取消、失败和完成状态。
- 支持节点级重试和失败原因分类。
- 应用重启后恢复可恢复任务。

### 监督式审批

- 工作流创建前生成费用估算。
- 付费节点自动插入审批门。
- 审批内容包括模型、数量、分辨率、时长、预计费用和影响节点。
- 未审批时后端不得调用付费 Provider。
- 批量任务、外部同步、发布和最终交付同样必须审批。

### 不可变运行快照

每次运行保存：

- 图 revision。
- 所选节点。
- 节点输入。
- 节点配置。
- Prompt 版本。
- Provider 和模型。
- 种子、分辨率、时长等参数。
- 预计与实际费用。
- 输出资产 ID。
- 错误、重试和审批记录。

### 验收条件

- 专业流程和无限画布能够无损切换。
- 失败节点可以单独重跑。
- 已批准且输入未变化的上游节点不会重复付费执行。
- 付费节点未经确认无法进入 Provider 调用。

## 阶段 C：剧本与分镜升级

### 标准生产链

`Brief → 故事结构/节拍 → 剧本版本 → 场次 → 镜头表 → 分镜候选 → 人工接受`

### 剧本模块

- 保存创意目标、受众、平台、时长、画幅、语言、品牌要求和禁用内容。
- 支持保守润色、平衡优化和结构重写三种强度。
- 所有 AI 修改生成候选版本，不覆盖原剧本。
- 支持差异审阅、局部接受和回退。
- 支持对白时长估算和目标总时长校验。

### 镜头结构

每个镜头至少保存：

- 稳定镜头 ID。
- 所属场次。
- 叙事目的。
- 时长。
- 景别。
- 机位。
- 运镜。
- 构图。
- 人物动作和表演。
- 对白或旁白。
- 光线和色彩。
- 风格。
- 首帧/尾帧要求。
- 所需角色、场景、道具和融合资产。
- 声音、环境、音乐与音效需求。
- 相邻镜头连续性要求。

### 自动检查

- 角色、场景和道具缺口。
- 轴线与视线连续性。
- 对白与镜头时长冲突。
- 镜头节奏过快或过慢。
- 服装、发型、天气、时间和道具状态连续性。
- 首尾帧无法衔接。
- 镜头生成器能力不匹配。

### 验收条件

- 从一句创意可以得到可编辑的结构化剧本和镜头表。
- 用户可在付费媒体生成前逐层接受或退回。
- 被接受的版本不会被后续生成静默覆盖。

## 阶段 D：统一资产与素材融合

### 资产库

统一管理：

- 角色。
- 场景。
- 道具。
- 产品。
- 风格。
- 声音。
- 音乐和音效。
- 融合资产。

每个资产保存逻辑 ID、等级、角色用途、批准版本、身份锚点、参考图、Prompt、QA、来源、许可证/授权状态和镜头依赖。

### 资产一致性

- 角色保存正面、侧面、近景、全身、服装和表情锚点。
- 场景保存空间结构、时间、天气、灯光和允许机位。
- 道具保存形状、材质、比例、文字和品牌细节。
- 产品资产必须单独标记不可修改的包装、Logo 和文字区域。

### 融合节点

每张参考图必须指定用途：

- 身份。
- 服装。
- 动作。
- 构图。
- 场景结构。
- 风格。
- 光线。
- 产品结构。

禁止把多张参考图无角色区分地传给生成模型。

### 候选对比台

- 同 Prompt 多模型并行生成。
- 同模型多种子和参数对比。
- A/B 或多宫格检查。
- 支持放大、评分、批注、批准、退回和局部重建。
- 批准新版本只切换 active 指针，历史版本仍可追溯。

### 失败闭环

- 技术失败：重新上传或重新调用。
- Prompt 问题：修订 Prompt。
- 身份或结构严重漂移：重建 Prompt，不继续小修重试。
- 融合连续失败达到阈值：强制重新设计融合方案或人工审阅。
- 任何失败都保留原候选、输入和 QA 报告。

### 阶段 D 实现状态（2026-08-18）

- [x] 统一资产库：角色、场景、道具、产品、风格、音频、音乐、音效、视频和融合资产。
- [x] 资产元数据：等级、用途、身份锚点、Prompt、来源、许可证/授权、保护区域和镜头依赖。
- [x] 引用角色约束：身份、服装、动作、构图、场景结构、风格、光线和产品结构。
- [x] 资产版本、QA、候选对比、评分/批注和 active 指针历史追溯。
- [x] 融合门：基础角色/场景/道具资产完成登记且引用角色明确后才允许进入融合。
- [x] V3 资产库界面、融合门检查和候选评审入口。

### 阶段 D 核查记录（2026-08-19）

- [x] Python 资产/服务/V3 测试通过；阶段 D 相关资产测试覆盖版本、active 指针、Prompt QA/Image QA 分离、引用角色、融合门、失败阈值和候选评审。
- [x] Node 前端测试通过；资产就绪门、融合阻断、候选失败处理、媒体解析和项目切换刷新均保持通过。
- [x] 未发现批准资产原地覆盖、无角色参考混传或失败候选丢失问题；阶段 D 可作为阶段 E 的输入边界。

## 阶段 E：Provider 与模型路由

### 统一 Provider Adapter

每个适配器必须实现：

- 健康检测。
- 凭据状态。
- 模型目录和能力发现。
- 输入格式和限制。
- 输出类型。
- 同步或异步任务模式。
- 费用估算。
- 进度查询。
- 取消。
- 重试策略。
- 错误分类。
- 输出资产标准化。

### 能力分类

- 文本编排。
- 视觉理解。
- 文生图。
- 图像编辑。
- 视频生成。
- TTS。
- 音乐。
- 音效。
- 口型同步。
- 超分。
- 插帧。
- 上传。
- 后期处理。

### 第一波正式适配

- OpenAI。
- OpenAI-compatible。
- OpenCode。
- 火山方舟 / Seedance。
- 本地 ComfyUI API。

### 第二波适配

仅在存在官方 API、官方文档和用户凭据时启用：

- Gemini / Veo。
- Runway。
- Kling。
- Minimax / Hailuo。
- Vidu。
- ElevenLabs。

### 外部平台边界

- TapNow、FlowMuse 等没有稳定公开 API 的平台仅作为产品研究对象。
- 不抓取私有页面，不模拟未公开接口，不保存用户平台密码。
- LibTV 可以通过官方 CLI/Skill 增加可选桥接，但必须使用其官方登录态。

### 路由策略

路由综合考虑：

- 用户是否锁定模型。
- 模型可用性。
- 输入画幅和时长限制。
- 质量预设。
- 隐私要求。
- 预计成本。
- 预计耗时。

身份关键镜头、产品关键镜头和已经批准的生成路径不得未经确认自动换模型。

### 阶段 E 实现状态（2026-08-19）

- [x] 新增统一 Provider Adapter 契约，覆盖健康检测、凭据状态、模型目录、能力发现、输入限制、输出类型、同步/异步模式、估价、进度、取消、重试策略、错误分类和输出标准化。
- [x] 完成第一波 OpenAI、OpenAI-compatible、OpenCode、火山方舟 / Seedance、ComfyUI 本地 API 适配器注册；ComfyUI 支持 `/system_stats`、`/object_info`、`/prompt`、`/history` 和 `/interrupt`。
- [x] V3 Provider 目录、契约、探测和路由预览 API 已接入；路由会校验 Provider 能力、启用状态、模型目录、隐私模式和输入限制，并返回标准估价与任务模式。
- [x] 目录、探测和标准化输出不包含 credential_ref、API key 或 Authorization 内容；凭据继续只从系统凭据库读取。
- [x] 新增 Provider 契约和 API 集成测试；Python 测试总数 73 项通过，Node 测试 39 项通过。

### 阶段 E 核查记录（2026-08-19）

- [x] 五类第一波 Provider 适配器均通过统一契约测试；健康、目录、能力、估价、任务控制和输出标准化保持一致。
- [x] Provider 目录、探测、路由预览和 Agent 编排调用均经过凭据脱敏边界；项目快照不保存 API key、Authorization、Cookie、Token 或 credential_ref 明文。
- [x] V3 Provider API 与现有旧版助手路径回归通过；未调用真实付费媒体 Provider，真实凭据仍保留在系统凭据库边界内。

## 阶段 F：Agent 编排

### Agent 输入

- 用户消息。
- 显式选择的画布节点。
- 项目规格。
- 已批准资产。
- 当前流程状态。
- 费用和执行边界。

### Agent 输出

Agent 不直接任意修改项目，只能提交结构化补丁：

- 新增节点。
- 修改节点配置。
- 新增或修改连接。
- 创建剧本/Prompt 候选版本。
- 建议运行节点。
- 建议审批门。

补丁预览必须显示：新增、修改、删除、保留和潜在费用。

### 默认自动化边界

可以自动执行：

- 文本分析。
- 剧本和 Prompt 候选草拟。
- 资产缺口检测。
- 镜头连续性检查。
- 节点编排和费用估算。

必须确认：

- 任何付费媒体调用。
- 批量生成。
- 替换 active 资产版本。
- 外部同步。
- 发布。
- 最终成片交付。

### 阶段 F 实现状态（2026-08-19）

- [x] 新增 Agent plan、事件和候选版本持久化迁移（schema v5），保存完整但脱敏的输入快照、基线 revision、结构化补丁和预览。
- [x] Agent 通过统一 Provider Adapter 的编排能力调用，输出统一为严格的 `AgentPatchV3`；旧版助手结果也会转换为待审阅计划。
- [x] 补丁支持节点/连接增改删、剧本/Prompt/简报/分镜候选、建议运行节点和审批门，并拒绝锁定节点修改、revision 冲突和非法执行图。
- [x] 预览显示新增、修改、删除、保留、候选版本、潜在费用、批量生成门和需要确认的动作；应用只写入图和候选版本，不直接执行媒体或替换 active 资产。
- [x] V3 无限画布与旧版助手均已接入“生成计划 → 预览 → 人工应用/拒绝”闭环；应用前检查项目与图 revision，候选替换保留为后续确认动作。
- [x] 新增 Agent 快照、补丁预览、锁定节点、revision 冲突和结构化输出脱敏测试；全量 Python 测试 76 项、根目录 Node 测试 39 项、V3 Vitest 测试 5 项通过，V3 生产构建通过。

## 阶段 G：可交付后期时间线

### 轨道

- 主视频。
- 叠加视频。
- 对白。
- 配乐。
- 环境声。
- 音效。
- 字幕。

### 编辑能力

- 镜头排序。
- 裁切和分割。
- 移动和吸附。
- 播放速度。
- 画幅适配。
- 基础转场。
- 音量。
- 淡入淡出。
- 字幕时码和样式。
- 画面/字幕安全区。
- 从已批准镜头自动装配初始时间线。

### 代理预览

- 大视频自动生成代理、缩略图和波形。
- 编辑预览使用代理文件。
- 最终导出自动切回原始批准素材。
- 代理文件可以重建，不影响主资产和时间线版本。

### FFmpeg 导出

导出前生成不可变 manifest，记录：

- 项目 ID。
- 时间线 revision。
- 所有输入资产 ID 和哈希。
- 轨道和片段时码。
- 编码参数。
- 分辨率和 FPS。
- 音轨映射。
- 字幕版本。
- 导出时间。

输出包括：

- MP4 成片。
- 字幕文件。
- 项目 JSON。
- 资产清单。
- 制作报告。
- Manifest。

第一版不实现专业调色、遮罩跟踪、复杂关键帧特效和第三方插件特效系统。

### 阶段 G 实现状态（2026-08-20）

- [x] 新增时间线事件、渲染作业和代理记录迁移（schema v6）；时间线保存使用 revision，并保留不可变更新事件。
- [x] 时间线支持主视频、叠加视频、对白、配乐、环境声、音效和字幕轨道；片段保存起始时码、时长、源内点、速度、音量、淡入淡出、转场和元数据。
- [x] 增加批准镜头自动装配；只读取已批准镜头的显式 artifact 引用，不修改项目源文档、active 资产或原始媒体。
- [x] V3 时间线界面支持片段拖拽十帧吸附、数值裁切/移动、速度、音量、转场、字幕编辑、分割和删除。
- [x] 增加代理 API；代理可重建，不覆盖主资产，并尝试生成缩略图与音频波形。
- [x] 增加 `/api/v2/renders/estimate`、`/api/v2/renders`、审批、取消和状态 API；未确认时不启动 FFmpeg。
- [x] 渲染使用时间线快照和输入 artifact SHA-256 生成不可变 manifest；最终输出使用原始批准素材，不把代理用于交付。
- [x] 交付物包含 MP4、SRT、项目 JSON、资产清单、制作报告和 manifest，并通过当前项目文件 URL 返回。
- [x] 新增 V3-only 交付测试覆盖根入口、旧入口退役、时间线装配、revision、外部路径阻断、渲染审批和输出路径安全。

## 阶段 H：质量、性能和安全

### 数据安全

- 所有项目更新使用 revision 冲突检测。
- 任何迁移、导入和覆盖操作先验证目标项目。
- 已批准资产只能被新版本替代，不能原地覆盖。
- 不批量删除媒体或目录。
- 项目删除继续受最后项目和活动任务保护。

### 凭据安全

- API 密钥只存系统凭据库。
- 前端、SQLite、日志、SSE、项目导出和错误响应都不包含明文凭据。
- Provider 日志必须脱敏 Authorization、Cookie、Token 和签名参数。

### 性能目标

- 300 个节点的画布可流畅缩放和拖拽。
- 100 个镜头和 1000 个资产的项目使用虚拟化列表。
- 图保存使用增量或压缩后的结构化 JSON。
- 媒体预览使用缩略图和代理，不一次性加载原始文件。
- SSE 或 WebSocket 只传输状态变化，不重复发送完整项目。

### 阶段 H 实现状态（2026-08-20）

- [x] 根入口只服务 V3 构建产物；旧 `/api` 工作台接口统一返回退役响应，V3 前端只调用 `/api/v2`。
- [x] 时间线渲染与代理输入均验证 artifact 属于当前项目且文件位于项目媒体目录内；输出文件名和项目路径拒绝目录穿越。
- [x] V3 render 快照只保存结构化配置、输入 hash、Provider/模型字段和交付结果，不写入 credential_ref、Authorization、Cookie、Token 或 API key。
- [x] 代理、时间线和 render 作业均可在应用重启后从数据库状态恢复或重新读取；代理文件按源 hash/preset 缓存。
- [x] 已完成 Python V3 交付测试、Node/Vitest 测试和 Vite 生产构建；旧版测试集合需按 V3-only 合同迁移，不再作为旧接口兼容门槛。
- [ ] 300 节点 / 1000 资产真实项目的浏览器性能基准和完整端到端真实 Provider 演练仍需在目标机器上执行；本地 Mock/零费用链路已具备。

## 6. 公共 API 设计

新增并唯一使用版本化 `/api/v2`；旧 `/api` 工作台接口在 V3 运行时退役，系统健康检查、媒体文件读取等基础路径除外。

### 项目图

- `GET /api/v2/projects`
- `GET /api/v2/projects/{project_id}`
- `PUT /api/v2/projects/{project_id}`
- `GET /api/v2/projects/{project_id}/graph`
- `PUT /api/v2/projects/{project_id}/graph`

更新请求必须包含：

```json
{
  "expected_revision": 3,
  "graph": {
    "version": 1,
    "nodes": [],
    "edges": [],
    "viewport": {},
    "metadata": {}
  }
}
```

### 工作流模板

- `GET /api/v2/workflow-templates`
- `POST /api/v2/workflow-templates`
- `POST /api/v2/projects/{project_id}/apply-template`

### 运行与估价

- `POST /api/v2/runs/estimate`
- `POST /api/v2/runs`
- `GET /api/v2/runs/{run_id}`
- `GET /api/v2/runs/{run_id}/events`
- `POST /api/v2/runs/{run_id}/approve`
- `POST /api/v2/runs/{run_id}/pause`
- `POST /api/v2/runs/{run_id}/resume`
- `POST /api/v2/runs/{run_id}/cancel`

### Provider

- `GET /api/v2/providers/catalog`
- `POST /api/v2/providers/{id}/probe`
- `POST /api/v2/providers/route-preview`

### 资产血缘

- `GET /api/v2/artifacts/{artifact_id}/lineage`
- `POST /api/v2/artifacts/{artifact_id}/lineage`
- `GET /api/v2/projects/{project_id}/assets`

### 时间线与导出

- `GET /api/v2/projects/{project_id}/timeline`
- `PUT /api/v2/projects/{project_id}/timeline`
- `POST /api/v2/projects/{project_id}/timeline/assemble`
- `POST /api/v2/projects/{project_id}/proxies`
- `GET /api/v2/proxies/{proxy_id}`
- `POST /api/v2/renders/estimate`
- `POST /api/v2/renders`
- `GET /api/v2/renders/{render_id}`
- `POST /api/v2/renders/{render_id}/approve`
- `POST /api/v2/renders/{render_id}/cancel`

## 7. 核心类型

### WorkflowNode

- `id`
- `kind`
- `label`
- `position`
- `config`
- `inputs`
- `outputs`
- `status`
- `version`
- `locked`

### WorkflowEdge

- `id`
- `source`
- `target`
- `source_port`
- `target_port`
- `relation`

### WorkflowRun / NodeRun

- 稳定运行 ID。
- 项目 ID。
- 图 revision。
- 状态。
- 输入快照。
- 估价。
- 实际费用。
- 输出。
- 错误。
- 尝试次数。
- 开始/结束时间。

### ArtifactRef

- 资产 ID。
- 逻辑资产 ID。
- 项目 ID。
- 类型。
- 版本。
- 文件路径。
- MIME。
- SHA-256。
- Provider 和模型。
- Prompt 版本。
- QA 决策。
- 授权状态。
- 父资产和派生关系。

### TimelineDocument

- Revision。
- 宽高。
- FPS。
- 总时长。
- 轨道。
- 片段。
- 字幕。
- 音频参数。
- 输出预设。

## 8. 测试计划

### 数据迁移

- V1、V2 和 V3 数据库升级。
- 空项目、正常项目和百镜头项目。
- 未知 JSON 字段保留。
- 迁移中断。
- 重复迁移幂等。
- V3 回滚后 V2 数据仍可读。

### 图验证

- 重复节点 ID。
- 重复连接 ID。
- 不存在的起点或终点。
- 执行自环。
- 多节点执行环。
- 参考环允许保存。
- Revision 冲突。

### 调度器

- 串行链。
- 并行分支。
- 多上游汇合。
- 局部运行。
- 缓存命中。
- 上游修改后的依赖失效。
- 暂停、恢复和取消。
- 应用重启后的任务恢复。
- 节点级重试。

### 费用与审批

- 无付费节点可直接排队。
- 付费节点未确认时只创建审批门。
- 估价改变后原审批失效。
- 批量任务必须审批。
- 后端直接调用同样无法绕过审批。

### Provider

- Mock 合约测试。
- 鉴权失败。
- 限流。
- 超时。
- 模型下线。
- Provider 返回成功但缺少媒体 URL。
- 取消失败。
- 异步轮询恢复。
- 敏感信息脱敏。

### 资产

- 版本登记和 active 切换。
- Prompt QA 与媒体 QA 分离。
- 父子血缘。
- 多参考角色。
- 批准资产不可覆盖。
- 失败候选保留。
- 融合失败阈值。

### 前端

- 专业流程与画布同步。
- 节点移动、连接、删除、撤销和保存。
- 图冲突提示。
- 多选局部运行。
- 费用确认。
- 断网和恢复。
- 画布键盘操作和基础可访问性。

### 时间线

- 片段排序和裁切。
- 画幅适配。
- 音画同步。
- 字幕时码。
- 代理与原片切换。
- 缺失素材。
- FFmpeg 中断。
- Manifest 输入哈希。

### 端到端验收

1. 使用 Mock Provider 完成零费用全流程。
2. 使用已配置的真实文本 Provider 完成剧本和分镜。
3. 使用真实图片 Provider 生成并批准资产。
4. 使用真实视频 Provider 生成一个镜头。
5. 使用真实 TTS 生成并批准一句对白。
6. 将批准素材装入时间线并导出 MP4 与 manifest。

## 9. 持续优化机制

固定五个真实任务作为每轮基准：

1. 从创意到批准分镜。
2. 修复跨镜头角色漂移。
3. 多模型生成对比并批准候选。
4. 恢复失败或中断的生成任务。
5. 从批准镜头到最终导出。

每轮记录：

- 用户操作步数。
- 人工等待时间。
- 生成耗时。
- 失败率。
- 费用。
- 返工次数。
- 数据或版本冲突。

目标是在不降低审批、QA 和追溯能力的前提下，相对当前工作台减少至少 30% 的重复手工操作。

每个阶段遵循：

`实现 → 自动测试 → 实际项目演练 → UI/流程复盘 → 更新计划 → 下一阶段`

任何阶段若出现数据丢失、付费绕过、凭据泄露或批准资产被覆盖，应停止后续阶段并先修复根因。

## 10. 发布与回退

- V3 直接作为 `/` 默认入口。
- 旧工作台不作为运行时回退入口；需要回退时回退到 V3 数据库备份和 V3 构建版本。
- 达到 V3 自身的完整端到端验收后发布，不等待旧版功能对等。
- 旧 API 请求返回明确的退役错误，不静默转发到旧逻辑。
- 数据库迁移采用只增不删策略。
- 不自动迁移或删除媒体文件。
- 不批量删除旧前端和旧 API。
- 正式发布前生成数据库备份、项目导出和迁移报告。

## 11. 明确排除项

以下内容不进入 V3 第一版：

- 团队账号和组织权限。
- 多人实时协作。
- 云端跨设备同步。
- 自动发布到社交平台。
- 专业调色系统。
- 遮罩跟踪和复杂合成。
- 完整关键帧动画系统。
- 未公开或逆向的平台 API。
- 未经过许可证审核的第三方代码。

## 12. 实施安全约束

- 当前工作区所有已有修改都视为用户资产。
- 禁止使用 `git reset --hard` 或强制覆盖。
- 禁止批量删除文件或目录。
- 不批量删除旧数据库、媒体目录或旧源文件；若确需清理，必须逐个明确路径并单独验证。
- 新迁移必须具备测试和回退路径。
- 修改重叠文件前先检查现有差异。
- API 密钥和凭据不得写入仓库。
- 所有付费调用继续由后端强制确认。

---

本文件保留完整规划与恢复实施记录。阶段 D、阶段 E 已通过核查，阶段 F 已完成监督式 Agent 编排闭环；后续从阶段 G 的可交付后期时间线开始。当前产品口径为 V3-only：根入口和 `/api/v2` 是唯一运行时表面，旧版入口与旧版接口退役。
