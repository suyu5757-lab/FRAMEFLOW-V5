# FRAMEFLOW Voice Controller 接入与流程审计

日期：2026-08-13

## 已完成

- 将 `voice-controller` 加入九阶段生产流水线，位置为镜头导演之后、Seedance 打包之前。
- 新增“配音与声音”工作区及未完成数量徽标。
- 为旧项目自动补充声音数据模型，并把旧“音频”资产路由到 Voice Controller。
- 建立声音、参考声音、对白、Take、配乐 Cue、音效、环境声和 `@Audio` 映射数据结构。
- 支持预设声音、Voice Design 和声音克隆三类声音来源的登记。
- 支持参考声音本地上传、格式限制、SHA-256 和授权状态登记。
- 支持 OpenAI TTS 试听生成、逐句 Take 版本、音频播放、QA 批准和正式 Take 选择。
- 支持配乐 Cue、对白避让、版权状态和 QA 门禁。
- 支持 Seedance `@Audio1` / `@Audio2` / `@Audio3` 映射。
- 支持导出 Voice Controller 兼容的音频生产包 JSON。
- 交付报告新增声音状态。
- 服务端 API Key 仅从环境变量读取，前端和项目数据不存储密钥。

## 当前流程结论

```text
剧本与分镜
-> 资产监管
-> 角色 / 场景 / 道具 / 融合
-> 镜头导演给出时长、情绪、cut-in / cut-out 和声桥
-> Voice Controller 完成选角、授权、Take、配乐 Cue 和音频 QA
-> 资产监管登记声音就绪
-> Seedance 打包映射批准的 @Audio
-> 视频生成
-> 视频 QA
-> 剪辑与最终混音
```

声音工作采用两次进入：剧本锁定后完成声音选角和试音，镜头时长锁定后完成正式 Take 和音乐 Cue。

## 测试结果

### 自动化

- Python 语法检查：通过。
- JavaScript 语法检查：通过。
- 服务健康接口：通过。
- 私有服务文件不可访问：通过。
- TTS 参数白名单：通过。
- 缺少 API Key 时停止外呼：通过。
- 模拟 TTS WAV 生成、时长读取和文件登记：通过。
- 参考声音本地上传、哈希和时长读取：通过。
- 敏感 API Key 扫描：未发现硬编码密钥。

### 浏览器端到端

- 旧项目音频数据迁移：通过。
- 九阶段流水线和导航：通过。
- 未确认付费调用时零请求：通过。
- 未授权克隆声音无法锁定：通过。
- Take QA 后才能选为正式 Take：通过。
- 正式 Take 生成 `@Audio1` 映射：通过。
- 390px 移动端页面：通过。
- 浏览器控制台：无错误或警告。
- Lighthouse 移动端快照：Accessibility 100、Best Practices 100、SEO 100、Agentic Browsing 100。

## P0：正式生产前必须补齐

### 1. 持久化从 localStorage 升级为项目文件

当前浏览器 `localStorage` 仍是项目主存储。它不适合大量音频、跨设备协作或长期版本追踪。

建议：

- 服务端保存 `projects/PRJ001/project.json`。
- 音频保存到 `projects/PRJ001/audio/`。
- 浏览器只保存最近项目和 UI 偏好。
- 增加导入、备份、恢复和冲突检测。
- 每次批准资产写入不可覆盖的版本记录。

### 2. 把“手动 QA 按钮”升级为真实检测

当前 QA 是流程门禁，不代表已经执行信号分析。

建议接入 FFmpeg/ffprobe：

- 实测时长、采样率、声道、编码和文件完整性。
- 检测静音、截断、削波和异常峰值。
- 测量响度和 true peak。
- 生成波形和试听定位点。
- 语音转写后与原对白比对漏字、多字和专有名词。
- 审批界面逐项显示测量证据，不能一键假定全部通过。

### 3. 完整授权证据链

当前可登记授权状态和参考文件哈希，但没有授权材料管理页面。

建议：

- 单独保存同意录音或授权文档，不与普通参考音混放。
- 记录授权人、用途、商业使用、语言、地区、期限和撤销条件。
- 增加 `consent-supplied -> consent-verified` 的人工复核动作。
- 声音撤销后自动阻塞所有派生声音和未交付 Take。
- 不在浏览器 localStorage 保存敏感授权材料。

### 4. 服务进程和端口管理

启动器目前没有检测旧进程、版本和端口占用。

建议：

- 启动前调用 `/api/health` 检查版本。
- 端口被旧版服务占用时给出明确提示。
- 健康接口返回工作台版本、音频接口版本和配置状态。
- 增加“安全重启服务”按钮或独立启动器。

## P1：下一轮应实现

### 1. 供应商适配器

保留当前供应商中立模型，逐个增加：

- OpenAI：试听与导演式 TTS。
- MiniMax：中文正式 TTS、Voice Design 和授权克隆。
- ElevenLabs：Speech-to-Speech 表演迁移。
- CosyVoice：本地或私有部署。
- 音乐供应商：只有在商业使用和输出权利明确后接入。

每个适配器都应统一返回供应商、模型、任务 ID、费用单位、设置、文件和错误分类，不在 UI 内硬编码具体业务规则。

### 2. 对白自动提取和同步

- 从结构化剧本提取角色、对白、旁白和所属镜头。
- 修改剧本文字后标记旧 Take `stale`，不能继续作为正式版本。
- 镜头时长改变后自动提示重新检查目标时长。
- 建立专有名词和多语言发音词典。

### 3. 真正的 Take 比较台

- A/B/C 同句试听。
- 波形、时长差、供应商和成本并排显示。
- 单词级局部替换。
- 保留 dry、enhanced 和 final 三种角色。
- 批准版本只读；修订必须产生新版本。

### 4. 配乐和混音交接

- 增加音乐参考的抽象特征提取，避免直接仿制具体作品。
- 增加配乐进入、变化、高潮、退出和循环点可视化。
- 支持无旋律、无鼓、低强度和短结尾替代版本。
- 导出 Dialogue、Narration、Music、Ambience、SFX 分轨。
- 先输出 WAV + CSV/SRT，后续再考虑 AAF/XML。

## P2：扩展能力

- 批量任务队列、并发限制、失败重试和暂停恢复。
- 逐供应商成本预算、实际费用和字符/分钟统计。
- 说话人分离、自动转写、情绪识别和口型对齐。
- 本地 CosyVoice GPU 服务和模型版本管理。
- 多语言配音、翻译审校和跨语言声音一致性。
- DaVinci Resolve/Premiere 工程交接。
- 项目级审计日志和角色权限。

## 建议的下一步

优先完成“项目文件持久化 + FFmpeg 真实音频检测”。这是从可用原型进入可靠生产工具的关键一步；在这两项完成前，不建议批量接入多个付费供应商。
