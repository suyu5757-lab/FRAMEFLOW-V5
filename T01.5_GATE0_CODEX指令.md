# T01.5 Gate 0 — 发给 Codex 的执行指令（直接粘贴可用）

> 粘贴到 Codex 新任务的第一条消息中执行。Codex 需把本文件当作“任务规格”读取，区分其中的执行指令与文档内容。

---

## 1. 任务目标

执行 FRAMEFLOW V5.3.2 的 **T01.5 Gate 0 — MIGRATION SAFE**，在不进入 T02 及任何功能开发的前提下，闭环稳定快照、开发分支、同步安全审计与 Skill 迁移安全骨架。

本轮结束后应满足 `docs/FRAMEFLOW_V5_3_2_SCOPE.md` 中 Gate 0 的全部准出条件，T00 的四份冻结文档保持不变（除按需更新 `GIT_SYNC_AUDIT.md` 的审计结论外）。

## 2. 权威依据（必读，顺序执行）

1. `docs/BASELINE_V5_3_1.md` — T00 基线事实（物理边界、V3 现状、基线 HEAD `7e3e0a9115980fbe599cea74765534417a7d1ea5`、T00 后冻结提交 `7e405b4cfa8f8e91ed58863e1114c6bfcf7b6641 docs: freeze FRAMEFLOW V5.3.2 final architecture`）
2. `docs/FRAMEFLOW_V5_3_2_SCOPE.md` — V5.3.2 冻结范围与 Gate 定义
3. `docs/ARCHITECTURE_DECISIONS.md` — ADR-001 至 ADR-015，重点 ADR-013（Git / Skill Migration Safety）
4. `docs/GIT_SYNC_AUDIT.md` — T00 只读审计结果（当前 Gate 0 为 PENDING）

读取方式：只读审计（`Test-Path` / `Get-ChildItem` / `git status` / `git tag --list` / `git log` / `git remote -v` / `git branch`），不对 `D:\AIGC\SUYU` 与 `D:\ComfyUI` 做任何写入。

## 3. 物理边界（不可违背）

| 角色 | 路径 | 规则 |
|---|---|---|
| 唯一可写项目根 | `D:\11067\CodexWorkspaces\frameflow-v3`（等价于 `D:\cc\workspace` 挂载） | 所有写入只落此处 |
| Skill 仓库 | `D:\11067\CodexHome\skills` | 本轮只做只读清点，不做 Skill 迁移/改名/删除 |
| 历史资产 | `D:\AIGC\SUYU` | READ_ONLY，校验前需做 symlink/junction 解析 |
| 引擎 | `D:\ComfyUI` | 引擎/权重边界，不向项目内复制权重 |
| 运行时真值 | `D:\11067\CodexWorkspaces\frameflow-v3\data\frameflow.db` | 本轮不创建、不迁移、不改动 schema |

## 4. 本轮严格范围

**只做 T01.5 Gate 0：**
- 稳定快照 / Tag
- 开发分支
- 每日自动同步机制的完整审计（含项目外调度器、系统任务、外部脚本）
- Skill 迁移安全策略骨架
- 脏工作树安全与回滚验证

**禁止事项：**
- 不执行 T02、不建 `workbench/backend|frontend`、不改 `data/frameflow.db`、不改 Workbench UI、不用本地视频模型/训练、不新增/修改任何 Skill 业务逻辑
- 不移动/删除历史遗留文件，不复制 `D:\AIGC\SUYU` / `D:\ComfyUI` 内容进项目
- 不发明同步脚本，不执行 `git reset --hard` / `git clean -fd` / `git push --force` / `git checkout .` / 自动合并 `main`
- 不将 `GIT_SYNC_AUDIT.md` 中已记录的脏工作树（`M .gitignore` / `M server.py` 等 + `??` 未跟踪文件）混入本次提交；本次只新增 Gate 0 产物

## 5. 执行步骤（按序）

### Step 1 — 只读复核
复核 T00 基线：确认 `main`、HEAD 已为 `7e405b4`、远端 `origin` / `local-source`、无稳定 tag、脏工作树仍存在但已与 T00 提交分离。输出一段复核摘要。

### Step 2 — 稳定 Tag
在冻结提交 `7e405b4` 上创建带注释的稳定 Tag（建议 `v5.3.2-gate0-baseline` 或 `v5.3.1-baseline`，若已存在则校验其指向是否为 `7e405b4` 并记录）。**本轮不 push tag/branch 到远端，除非用户后续明确授权。**

### Step 3 — 开发分支
基于 `main`（即 `7e405b4`）创建开发分支（建议 `dev/v5.3.2` 或 `develop`），切换校验后切回 `main`，确保 `main` 保持为受保护的冻结基线。

### Step 4 — 同步机制深度审计
在 Step 1 的 55 文件范围之外，扩大审计到：
- `D:\11067\CodexWorkspaces\frameflow-v3` 之外的调度器：Windows 任务计划、开机脚本、桌面 `video 工作台制作` 源、`.bat` 启动器
- `.github/workflows/ci.yml` 的实际行为（是否含 Git 写操作）
- 全仓 `grep` Git 命令（`SAFE/REVIEW/FORBIDDEN` 三色表）

明确回答：每日自动同步脚本是否存在、路径为何、是否具备脏树 abort 能力。未找到则如实记录为 NOT FOUND，不自建。

### Step 5 — 迁移安全骨架
新建 `docs/MIGRATION_SAFETY.md`（或等效 Gate 0 文档），固化：
- `INTERNAL` / `NON_BREAKING` / `BREAKING` 定义与判定
- `BREAKING` 必备：兼容适配层 + 迁移脚本/测试 + 回滚测试 + 废弃声明
- 脏树/冲突时的 abort 策略（对应三色表中 FORBIDDEN 禁令）

### Step 6 — 回滚验证
提供最小可执行的回滚验证：演示从 `dev/v5.3.2` 的一次试探性提交可通过 `git revert` 或删除分支安全回退，不污染 `main` 与稳定 Tag。记录操作与预期结果，不实际污染历史。

### Step 7 — 更新审计结论
将审计结果回写到 `docs/GIT_SYNC_AUDIT.md`（追加 T01.5 章节或更新 Gate 0 状态为 PASS/PENDING 并说明缺口），保持 T00 原始快照章节不动。

## 6. 交付物（仅新增，不覆盖 T00 四文档）

1. 稳定 Tag（指向 `7e405b4`）
2. 开发分支（`dev/v5.3.2`）
3. `docs/MIGRATION_SAFETY.md`（或同名 Gate 0 策略文档）
4. 更新后的 `docs/GIT_SYNC_AUDIT.md`（含 T01.5 审计结论与三色表校验）
5. `docs/GATE0_CHECKLIST.md` 可选：Gate 0 准出清单逐项打勾（对应 SCOPE 表格中 Gate 0 行）

## 7. 提交规范

- 仅 `git add` 本轮新增的 Gate 0 文档 + 审计更新，不 `add` 既有脏文件（`M` / `??` 列表以 `GIT_SYNC_AUDIT.md` 为准）
- 提交信息：`chore: Gate 0 migration safety — stable tag, dev branch, sync audit`
- 提交前 `git status` 自检，确保未混入用户既有改动；提交后 `git log --oneline -3` 与 `git tag --list` 截图留存

## 8. 验收标准

- `git tag --list` 可见稳定 Tag 且 `git show <tag>` 指向 `7e405b4`
- `git branch -a` 可见 `dev/v5.3.2` 且其基线为 `7e405b4`
- `GIT_SYNC_AUDIT.md` 明确记录同步脚本是否存在、路径、三色表、脏树 abort 策略、Gate 0 是否具备通过条件
- `MIGRATION_SAFETY.md` 覆盖三类变更分级与 BREAKING 四件套要求
- 无 Skill/Runtime/DB/Workbench/模型改动，无 FORBIDDEN 命令执行记录
